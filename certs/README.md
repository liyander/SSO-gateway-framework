Place TLS certificates here before running the gateway:

- fullchain.pem
- privkey.pem

For local testing, you can create temporary self-signed certificates. Replace them with trusted certificates before exposing the gateway publicly.
